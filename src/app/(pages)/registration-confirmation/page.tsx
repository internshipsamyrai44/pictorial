'use client';

export default function RegistrationConfirmation() {
  const style = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  };

  return <div style={style}>Registration Confirmation</div>;
}
